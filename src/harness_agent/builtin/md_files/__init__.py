"""Built-in markdown templates seeded into the user's workspace.

These files act as starting-point identity and memory documents. They are
copied into ``{workspace_dir}`` once via :py:meth:`HarnessAgent.init`
and never overwritten thereafter — users are expected to edit them.
"""

from __future__ import annotations
