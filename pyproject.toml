[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "orcakit-harness-agent"
# Single source of truth for the package version (runtime reads via importlib.metadata).
version = "1.0.10"
description = "Production-grade Harness Agent built on top of LangChain Deep Agents."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [{ name = "orcakit" }]
keywords = [
    "agent",
    "llm",
    "langchain",
    "langgraph",
    "deep-agents",
    "harness",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "deepagents>=0.7,<0.8",
    "langchain>=1.3.14,<2.0",
    "langchain-core>=1.5.0,<2.0",
    "langchain-openai>=1.2.2,<2.0",
    "langchain-anthropic>=1.5.4,<2.0",
    "langgraph>=1.0,<2.0",
    "langgraph-checkpoint-sqlite>=2.0",
    "httpx>=0.27",
    "markdownify>=0.13",
    "harness-memory>=0.9.6",
    "harness-browser>=0.7.7",
    "langchain-mcp-adapters>=0.3.0",
    # mcp 2.x breaks langchain-mcp-adapters (RequestContext import); pin until adapters support it.
    "mcp>=1.27.1,<2",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
# Fine-grained extras (prefer these in error messages / minimal installs)
cli = ["click>=8.1", "rich>=13.0", "prompt-toolkit>=3.0", "python-dotenv>=1.0"]
bedrock = ["langchain-aws>=0.2"]
remote-backends = ['deepagents-backends>=0.2; python_version >= "3.12"']
docker = ["docker>=7.0"]
opensandbox = ["opensandbox>=0.1.16,<0.2"]
observability = ["langfuse>=4.9.0"]
acp = ["agent-client-protocol>=0.9.0", "psutil>=5.9"]
# Aggregate extras (category installs; fine-grained extras remain preferred)
desktop = ["mss>=9.0", "pynput>=1.7", "pillow>=10.0"]
web-search-all = [
    "langchain-tavily>=0.1",
    "langchain-community>=0.3",
    "google-api-python-client>=2.0",
]
object-storage = [
    "cos-python-sdk-v5>=1.9",
    "oss2>=2.18",
    "esdk-obs-python>=3.24",
]
all = [  # library feature extras only; exclude cli — use [cli,all] when needed
    "langchain-aws>=0.2",
    "cos-python-sdk-v5>=1.9",
    "oss2>=2.18",
    "esdk-obs-python>=3.24",
    "mss>=9.0",
    "pynput>=1.7",
    "pillow>=10.0",
    "langchain-tavily>=0.1",
    "langchain-community>=0.3",
    "google-api-python-client>=2.0",
    'deepagents-backends>=0.2; python_version >= "3.12"',
    "docker>=7.0",
    "langfuse>=4.9.0",
    "agent-client-protocol>=0.9.0",
    "psutil>=5.9",
]

[project.scripts]
harness-agent = "harness_agent.cli.main:cli"

[project.urls]
Homepage = "https://github.com/TencentCloud/harness-agent"
Repository = "https://github.com/TencentCloud/harness-agent"
Issues = "https://github.com/TencentCloud/harness-agent/issues"
Changelog = "https://github.com/TencentCloud/harness-agent/blob/main/CHANGELOG.md"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "pytest-cov>=5.0",
    "ruff>=0.6",
    "mypy>=1.11",
    "pre-commit>=3.8",
    "types-requests",
    # CLI extras — required to collect/run tests under tests/cli/
    "click>=8.1",
    "rich>=13.0",
    "prompt-toolkit>=3.0",
    "python-dotenv>=1.0",
    # ACP extras — required by tests/test_acp_*.py
    "agent-client-protocol>=0.9.0",
    "psutil>=5.9",
    "mss>=9.0",
    "harness-browser>=0.7.7",
    "langfuse>=4.9.0",
]

# ---------------------------------------------------------------------------
# Build (hatch)
# ---------------------------------------------------------------------------

[tool.hatch.build.targets.wheel]
core-metadata-version = "2.3"
packages = ["src/harness_agent"]

[tool.hatch.build.targets.sdist]
core-metadata-version = "2.3"
include = [
    "src/harness_agent",
    "README.md",
    "CHANGELOG.md",
    "LICENSE",
    "pyproject.toml",
]

# ---------------------------------------------------------------------------
# Ruff
# ---------------------------------------------------------------------------

[tool.ruff]
line-length = 120
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
    "SIM", # flake8-simplify
    "RUF", # ruff-specific
    "N",   # pep8-naming
    "PL",  # pylint
]
ignore = [
    "E203",    # whitespace before ':'
    "PLR0913", # too many arguments
    "PLR2004", # magic value comparisons
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["PLC0415", "RUF012", "N803"]
"examples/**/*.py" = ["PLC0415", "RUF001", "T201"]
# Intentional lazy / optional imports across the package
"src/harness_agent/**/*.py" = ["PLC0415"]
# Complexity / specialty rules (keep narrow so new code still gets flagged)
"src/harness_agent/backends/__init__.py" = ["PLR0911", "PLR0912", "E402"]
"src/harness_agent/cli/**/*.py" = ["PLR0911", "PLR0912", "PLR0915"]
"src/harness_agent/llm/factory.py" = ["PLR0912"]
"src/harness_agent/config/env.py" = ["PLR0912"]
"src/harness_agent/config/__init__.py" = ["PLR0912"]
"src/harness_agent/protocols/langgraph.py" = ["PLR0912"]
"src/harness_agent/acp/**/*.py" = ["PLR0911", "N818", "RUF006"]
"src/harness_agent/builtin/tools/acp_runner.py" = ["PLR0911"]
"src/harness_agent/builtin/skills/**/scripts/*.py" = ["PLR0911", "PLR0912", "PLR0915"]
# i18n: full-width punctuation in user-facing zh strings / comments
"src/harness_agent/slash/core.py" = ["RUF001"]
"src/harness_agent/middleware/memory.py" = ["RUF003"]
"src/harness_agent/context_usage.py" = ["PLR0912"]

[tool.ruff.lint.isort]
known-first-party = ["harness_agent"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

# ---------------------------------------------------------------------------
# Mypy
# ---------------------------------------------------------------------------

[tool.mypy]
python_version = "3.11"
strict = true
warn_unused_ignores = true
warn_return_any = true
warn_redundant_casts = true
warn_unreachable = true
disallow_untyped_defs = true
no_implicit_optional = true
check_untyped_defs = true
files = ["src/harness_agent"]
exclude = [
    "src/harness_agent/builtin/skills/",
]

[[tool.mypy.overrides]]
module = [
    "markdownify.*",
    "deepagents.*",
    "yaml.*",
    "qcloud_cos.*",
    "oss2.*",
    "obs.*",
    "deepagents_backends.*",
    "docker.*",
    "opensandbox",
    "opensandbox.*",
    "mss.*",
    "langchain_tavily.*",
    "langchain_community.*",
    "langchain_aws.*",
    "googleapiclient.*",
    "harness_browser.*",
    "harness_memory.*",
    "langfuse.*",
    "rich.*",
    "prompt_toolkit.*",
    "boto3.*",
    "botocore.*",
    "psutil.*",
    "fastmcp.*",
    "acp.*",
    "defusedxml.*",
    "pymupdf.*",
    "pymupdf4llm.*",
]
ignore_missing_imports = true

# ---------------------------------------------------------------------------
# Pytest
# ---------------------------------------------------------------------------

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
]
asyncio_mode = "auto"
filterwarnings = [
    "error",
    "ignore::DeprecationWarning:pydantic.*",
    "ignore::DeprecationWarning:langchain.*",
]
 
[tool.coverage.run]
source = ["src/harness_agent"]
branch = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
    "if __name__ == .__main__.:",
]
